# Prerequisites
- docker

# Running the project
Within the project directory in the command line use:
```
docker-compose up --build
```

# Viewing the results
Open your browser to:
```
http://localhost:8080
```
